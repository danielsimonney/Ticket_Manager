{% extends 'base.html.twig' %}

{% block title %}edition Ticket{% endblock %}

{% block body %}

{{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">{{ button_label|default('save the message') }}</button>
    {{ form_end(form) }}

    {% endblock %}